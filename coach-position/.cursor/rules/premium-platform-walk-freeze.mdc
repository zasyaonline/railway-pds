---
description: Freeze walk distance/time style on both Coach Position TV URLs
globs: coach-position/public/css/style.css,coach-position/public/css/premium.css,coach-position/public/js/app.js,coach-position/public/index.html,coach-position/public/premium.html
alwaysApply: false
---

# Frozen — walk distance / time (both TV URLs)

Applies to:

- `/?station=…&display=…` (current TV)
- `/premium.html?station=…&display=…` (premium TV)

**Match the reference mockup exactly:**

1. Labels sit on the **platform side** of the yellow line (below it).
2. Left of You-are-here: `rotate(-90deg)`.
3. Right of You-are-here: `rotate(90deg)`.
4. Stack order: **meters** then **walk time** (`22m` / `walk 34 sec`).
5. Distances radiate outward from the pin in both directions.

Canonical CSS: `coach-position/public/css/style.css` (block marked `FROZEN (both TV URLs)`).

Do **not** change this unless the user explicitly asks.

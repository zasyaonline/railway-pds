{
  "name": "railway-coach-position",
  "version": "1.0.0",
  "description": "Coach Position display for large platform TVs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "refresh:live": "node scripts/refresh-live-cache.js",
    "publish:live": "bash scripts/publish-live-cache.sh",
    "publish:ui": "bash scripts/publish-ui.sh",
    "build:lambda": "bash scripts/build-lambda.sh",
    "deploy:aws": "bash scripts/deploy.sh"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
